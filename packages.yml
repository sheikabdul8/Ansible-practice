---
-  name: Installation of multiple packages
   hosts: all
   tasks:
     - name: Installation of packages using yum module
       yum: name={{ item }} state=present
       with_items:
         - net-tools
         - nmap
         - nginx
         - sysstat


